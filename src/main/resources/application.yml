spring:
  datasource:
    url: jdbc:oracle:thin:@localhost:1521:orcl
    username: businesstrip
    password: businesstrip123
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    database-platform: org.hibernate.dialect.Oracle10gDialect
    show-sql: true
    properties:
      hibernate.default_schema: businesstrip
      format_sql: true

---

spring.profiles: non-secure
